---
name: qa-pairs
slug: qa-pairs
description: "优先使用已发布的高频问答；无法可靠命中时转人工。"
---

# 高频问答与转人工

- 已发布且索引完成的高频问答由系统在 Agent 前自动精确匹配，命中后直接返回原答案，不再调用模型生成。
- 没有可靠命中时正常检索知识库；不要编造确定答案。
- 用户明确要求人工，或问题需要人工处理时，调用 `escalate_question`，携带必要的上下文。
- 不使用只读 SQL 工具；该工具按迁移计划延期为独立 Skill。
